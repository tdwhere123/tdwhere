name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify (npm ci + lint + test + build)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm run test

      - name: Typecheck + production build
        run: npm run build

      - name: Confirm SPA entry
        run: |
          test -f dist/index.html
          test -d dist/assets

      # E2E (scripts/e2e-smoke.mjs) stays manual: needs Chrome + WebGL-tolerant
      # headless environment and a preview server with /tdwhere/ base.
      # Run locally: npm run build && npm run preview & node scripts/e2e-smoke.mjs http://localhost:4173/tdwhere/
