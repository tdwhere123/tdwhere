/** Thin cobalt rule — section break for About (museum accent, not a card). */
import SharedCobaltRule from '@/components/CobaltRule'

export default function CobaltRule({ className }: { className?: string }) {
  return <SharedCobaltRule className={className} variant="about" />
}
